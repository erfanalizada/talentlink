name: CI/CD

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  # ---------------- AUTH SERVICE ----------------
  build-and-deploy-auth:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u erfanalizada --password-stdin

      - name: Build & push auth-service
        run: |
          docker build -t ghcr.io/erfanalizada/auth-service:${{ github.sha }} ./backend/auth-service
          docker push ghcr.io/erfanalizada/auth-service:${{ github.sha }}

      - name: Install OCI CLI
        run: pip install --upgrade pip && pip install oci-cli

      - name: Configure OCI CLI
        run: |
          mkdir -p $HOME/.oci
          echo "${{ secrets.OCI_CONFIG }}" | base64 --decode > $HOME/.oci/config
          echo "${{ secrets.OCI_KEY }}" | base64 --decode > $HOME/.oci/oci_api_key.pem
          chmod 600 $HOME/.oci/oci_api_key.pem

      - name: Configure kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Deploy auth-service
        run: |
          kubectl set image deployment/auth-service auth-service=ghcr.io/erfanalizada/auth-service:${{ github.sha }}
          kubectl rollout status deployment/auth-service
          kubectl get pods -o wide

  # ---------------- USER SERVICE ----------------
  build-and-deploy-user:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u erfanalizada --password-stdin
      - run: |
          docker build -t ghcr.io/erfanalizada/user-service:${{ github.sha }} ./backend/user-service
          docker push ghcr.io/erfanalizada/user-service:${{ github.sha }}
      - run: pip install --upgrade pip && pip install oci-cli
      - run: |
          mkdir -p $HOME/.oci
          echo "${{ secrets.OCI_CONFIG }}" | base64 --decode > $HOME/.oci/config
          echo "${{ secrets.OCI_KEY }}" | base64 --decode > $HOME/.oci/oci_api_key.pem
          chmod 600 $HOME/.oci/oci_api_key.pem
      - run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > $HOME/.kube/config
          chmod 600 $HOME/.kube/config
      - run: |
          kubectl set image deployment/user-service user-service=ghcr.io/erfanalizada/user-service:${{ github.sha }}
          kubectl rollout status deployment/user-service
          kubectl get pods -o wide

  # ---------------- JOB SERVICE ----------------
  build-and-deploy-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u erfanalizada --password-stdin
      - run: |
          docker build -t ghcr.io/erfanalizada/job-service:${{ github.sha }} ./backend/job-service
          docker push ghcr.io/erfanalizada/job-service:${{ github.sha }}
      - run: pip install --upgrade pip && pip install oci-cli
      - run: |
          mkdir -p $HOME/.oci
          echo "${{ secrets.OCI_CONFIG }}" | base64 --decode > $HOME/.oci/config
          echo "${{ secrets.OCI_KEY }}" | base64 --decode > $HOME/.oci/oci_api_key.pem
          chmod 600 $HOME/.oci/oci_api_key.pem
      - run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > $HOME/.kube/config
          chmod 600 $HOME/.kube/config
      - run: |
          kubectl set image deployment/job-service job-service=ghcr.io/erfanalizada/job-service:${{ github.sha }}
          kubectl rollout status deployment/job-service
          kubectl get pods -o wide

  # ---------------- CV SERVICE ----------------
  build-and-deploy-cv:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u erfanalizada --password-stdin
      - run: |
          docker build -t ghcr.io/erfanalizada/cv-service:${{ github.sha }} ./backend/cv-service
          docker push ghcr.io/erfanalizada/cv-service:${{ github.sha }}
      - run: pip install --upgrade pip && pip install oci-cli
      - run: |
          mkdir -p $HOME/.oci
          echo "${{ secrets.OCI_CONFIG }}" | base64 --decode > $HOME/.oci/config
          echo "${{ secrets.OCI_KEY }}" | base64 --decode > $HOME/.oci/oci_api_key.pem
          chmod 600 $HOME/.oci/oci_api_key.pem
      - run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > $HOME/.kube/config
          chmod 600 $HOME/.kube/config
      - run: |
          kubectl set image deployment/cv-service cv-service=ghcr.io/erfanalizada/cv-service:${{ github.sha }}
          kubectl rollout status deployment/cv-service
          kubectl get pods -o wide

  # ---------------- MATCHING SERVICE ----------------
  build-and-deploy-matching:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u erfanalizada --password-stdin
      - run: |
          docker build -t ghcr.io/erfanalizada/matching-service:${{ github.sha }} ./backend/matching-service
          docker push ghcr.io/erfanalizada/matching-service:${{ github.sha }}
      - run: pip install --upgrade pip && pip install oci-cli
      - run: |
          mkdir -p $HOME/.oci
          echo "${{ secrets.OCI_CONFIG }}" | base64 --decode > $HOME/.oci/config
          echo "${{ secrets.OCI_KEY }}" | base64 --decode > $HOME/.oci/oci_api_key.pem
          chmod 600 $HOME/.oci/oci_api_key.pem
      - run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > $HOME/.kube/config
          chmod 600 $HOME/.kube/config
      - run: |
          kubectl set image deployment/matching-service matching-service=ghcr.io/erfanalizada/matching-service:${{ github.sha }}
          kubectl rollout status deployment/matching-service
          kubectl get pods -o wide

  # ---------------- NOTIFICATION SERVICE ----------------
  build-and-deploy-notification:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u erfanalizada --password-stdin
      - run: |
          docker build -t ghcr.io/erfanalizada/notification-service:${{ github.sha }} ./backend/notification-service
          docker push ghcr.io/erfanalizada/notification-service:${{ github.sha }}
      - run: pip install --upgrade pip && pip install oci-cli
      - run: |
          mkdir -p $HOME/.oci
          echo "${{ secrets.OCI_CONFIG }}" | base64 --decode > $HOME/.oci/config
          echo "${{ secrets.OCI_KEY }}" | base64 --decode > $HOME/.oci/oci_api_key.pem
          chmod 600 $HOME/.oci/oci_api_key.pem
      - run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > $HOME/.kube/config
          chmod 600 $HOME/.kube/config
      - run: |
          kubectl set image deployment/notification-service notification-service=ghcr.io/erfanalizada/notification-service:${{ github.sha }}
          kubectl rollout status deployment/notification-service
          kubectl get pods -o wide

  # ---------------- FRONTEND ----------------
  build-and-deploy-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u erfanalizada --password-stdin
      - run: |
          docker build -t ghcr.io/erfanalizada/talentlink-frontend:${{ github.sha }} ./frontend
          docker push ghcr.io/erfanalizada/talentlink-frontend:${{ github.sha }}
      - run: pip install --upgrade pip && pip install oci-cli
      - run: |
          mkdir -p $HOME/.oci
          echo "${{ secrets.OCI_CONFIG }}" | base64 --decode > $HOME/.oci/config
          echo "${{ secrets.OCI_KEY }}" | base64 --decode > $HOME/.oci/oci_api_key.pem
          chmod 600 $HOME/.oci/oci_api_key.pem
      - run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 --decode > $HOME/.kube/config
          chmod 600 $HOME/.kube/config
      - run: |
          kubectl set image deployment/frontend frontend=ghcr.io/erfanalizada/talentlink-frontend:${{ github.sha }}
          kubectl rollout status deployment/frontend
          kubectl get pods -o wide
