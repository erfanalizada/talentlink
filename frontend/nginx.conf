server {
    listen 80;

    # Serve Flutter web app
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests to backend (ClusterIP service)
    location /api/ {
        proxy_pass http://backend-service:5000/;
    }
}
