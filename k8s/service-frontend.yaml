apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: ClusterIP              # Changed from LoadBalancer to ClusterIP
  selector:
    app: frontend
  ports:
    - port: 80                 # Service listens on port 80
      targetPort: 80           # Forwards to containerPort 80
