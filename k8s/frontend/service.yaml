apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: ClusterIP    # Only exposed internally; Ingress handles external access
  selector:
    app: frontend
  ports:
    - port: 80       # Service listens on port 80
      targetPort: 80 # Forwards to container port 80
